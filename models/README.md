# Model slot

`fa_IR-mana-medium.onnx` and `fa_IR-mana-medium.onnx.json` are intentionally not duplicated inside this package.

Run `python scripts/fetch_model.py` or `SETUP_ONCE.bat` to download the verified MIT-licensed Mana Persian Piper model.

Expected model SHA-256:
`e390c0e74ba71fd97c49ba662ee0c6e1724b462ba2d4561698af4f564840f126`
